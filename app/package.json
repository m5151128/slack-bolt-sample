{
  "devDependencies": {
    "@types/express": "^5.0.0",
    "nodemon": "^3.1.9",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.0",
    "dotenv": "^16.4.7"
  },
  "scripts": {
    "build:watch": "npx tsc --watch",
    "start:watch": "nodemon dist/app.js",
    "dev": "npm run build:watch & npm run start:watch"
  }
}
